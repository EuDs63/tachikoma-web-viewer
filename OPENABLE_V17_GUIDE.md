# Tachikoma v17 开舱与底盘

`tachikoma_v17_openable.blend` 在 v16 绑定与巡逻动画基础上新增了可开背舱和重制底盘。

## 开舱控制

在 Object Mode 选择 `TACHIKOMA_RIG`，进入 Custom Properties：

- `pod_top_hatch`：`0` 为顶盖关闭，`1` 为顶盖升起。
- `pod_rear_doors`：`0` 为左右后舱门关闭，`1` 为左右舱门完全打开。
- `pod_sensor_hatch`：`0` 为顶部矩形检修盖关闭，`1` 为检修盖向后打开约 102°。

三个属性均支持 0–1 中间值，也可右键插入关键帧。顶盖、灯组、舱门内衬、铰链和内部结构均继续跟随原 `CTRL_POD`；新底盘跟随 `CTRL_BODY`，原有四足 IK、手臂、轮式/步行切换与巡逻动作不变。

## 本版范围

- 把 v16 单一封闭背舱壳拆成固定壳体、升降顶盖和左右侧开舱门，并在顶盖上增加带真实开孔、加强筋与后铰链的独立检修盖。
- 空座舱包含座椅脊柱、头枕、脚舱、三块显示器、双操纵杆、侧垫、门内衬和顶盖双绿色传感器；按要求未制作人物。
- 原宽碟状机腹替换为中央分层轮毂、六根锥形放射支臂、六个球形关节、暗色颈圈与检修槽。

主要参考为 `references/BV1j3411K7WM_keyframes/keyframes` 中的 007–039 号开舱关键帧和 041 号底视关键帧。全部尺寸为视觉比例复原，不是实物测绘数据。
