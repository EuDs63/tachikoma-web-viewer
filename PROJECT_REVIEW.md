# Tachikoma 项目评估与优化方向

本次范围是网页 v22 的展示、交互和加载流程。底层模型保持 v21，已有 GLB 与 `.blend` 工程未改动，也没有重新导出模型。

## 判断与依据

项目最有价值的部分已经存在：完整的蓝白机械造型、独立开舱状态、可播放的巡逻动作，以及详细命名的部件层级。原页面将模型放在深色 HUD、扫描线和遥测信息之间，视觉强调偏向控制台；模型自身的轮廓、性格与内构还有更大的展示空间。

因此，当前阶段优先重做“如何观看”，并改善加载可靠性和闲置资源占用。几何减面与模型资产架构调整属于下一阶段，需要独立验证轮廓、关节和连接结构的质量。

以下数据直接读取现有 GLB 的 glTF JSON 元数据及 accessor 数量，文件大小使用十进制 MB。三角形和 primitive 数量是资产规模，不等同于浏览器实测 draw call 或帧率。

| 资产 | 大小 | 节点 | Mesh | Primitive | 三角形 | 材质 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WALK | 1.790 MB | 483 | 408 | 409 | 646,188 | 26 |
| WHEEL | 1.706 MB | 435 | 384 | 385 | 631,436 | 25 |
| OPEN | 1.790 MB | 483 | 408 | 409 | 646,188 | 26 |
| PATROL | 2.033 MB | 483 | 408 | 409 | 646,188 | 26 |

四个资产均使用 `KHR_draco_mesh_compression`，没有纹理图片。PATROL 包含一段名为 `Tachikoma_v02` 的动画，216 条 channel，时长 17.75 秒；`CTRL_ROOT` 平移轨道采样为 426 帧。其他三个资产是静态姿态，不能直接当作已有的姿态转换动画。

压缩后的文件不大，但约 64.6 万三角形和 409 个 primitive 仍需解码、提交和绘制。应分别测量网络、CPU 与 GPU 开销，避免把下载体积误当作渲染负担。

## 已实施的改进

### 视觉与内容

- 用纸白背景、克制的蓝色强调、大幅标题和留白建立展览式界面，让三维模型成为主角。
- 调整环境照明、主光、补光和接触阴影，保留蓝白角色特征，并提供夜间观察。
- 将姿态、视角、展示工具分为可辨认的操作区域，补充结构档案和操作指南。
- 适配窄屏布局，保留触控、键盘导航、焦点反馈和减少动态效果偏好。

### 交互与实现

- 保留四种模型模式，加入自动环绕、线框观察、PNG 保存和当前模型下载。
- 视角预设使用平滑过渡；取景同时考虑水平、垂直视场和模型深度，减少窄屏裁切。
- 巡逻支持播放、暂停和时间轴拖动；使用 `CTRL_ROOT` 抵消平面位移，替代每帧遍历模型包围盒以重置中心的方式。
- 静态状态按需渲染，页面隐藏或展品离开视口时停止更新；限制像素比，并降低动态阴影的更新频率。
- 对模型加载去重，保留最多两个已加载资产，释放被淘汰的几何、材质和纹理。
- 固定并本地保存 Three.js `0.180.0` 及必要依赖，移除查看器的 CDN 运行依赖；保留第三方许可证。
- 增加载入进度、失败重试和 3D 启动失败时的录像入口。

这些是实现方式的改进，没有据此宣称帧率提升比例、加载提速比例或移动端性能达标。

## 验证状态

已完成 GLB 元数据读取、本地模块相对导入依赖检查、Three.js 版本核对，以及页面 ID 与 JavaScript 选择器的一致性检查。缓存并发、暂停拖动和根位移处理也已接受静态审查。

已在内置 Chromium 浏览器完成以下手动检查：

- 四种模式均可载入；连续切换后，模型、选中状态、播放条和下载链接保持一致。
- 巡逻暂停后拖动至 65%，时间码正确显示 `00:11 / 00:17`。
- 正面、侧面、背面、透视与复位；自动环绕、线框和日间 / 夜间切换。
- 指南与截图预览弹窗；Escape 关闭后，焦点回到原按钮。截图预览生成有效的 927 × 491 PNG 图像。
- 320、390、820、1366 和 1440 像素宽度检查，未发现横向溢出；修复了最窄屏的导航换行与视角控件拥挤。
- 全屏进入与退出；正常页面控制台无错误或警告。
- 用独立本地测试服务模拟一次模型请求 503，重试后恢复；模拟 3D 模块请求 503，错误页保留录像入口，重试后恢复。

桌面与手机验收截图保存在本地 `audit/tachikoma-v22-desktop.png` 与 `audit/tachikoma-v22-mobile.png`（该目录不提交）。

尚未实测的边界：WebGL 上下文丢失后的实际恢复、真实低性能手机的帧率与显存、多轮切换后的资源占用曲线。PNG 图像生成及预览已验证，但内置浏览器自动化未返回下载文件事件，因此没有将下载到磁盘标记为验证通过。线上发布结果以 GitHub Pages 的部署记录为准。

## 下一阶段：按优先级推进

### 1. 降低几何与绘制提交成本

当前 WALK、OPEN 和 PATROL 均约 646,188 个三角形、409 个 primitive，是最值得先量化的性能方向。先记录浏览器实际 draw call、帧耗时和显存变化，再针对瓶颈调整：

- 合并相同材质、同一刚性运动部件上的几何，避免跨活动关节合并；保留热点所需的语义映射。
- 为远景和窄屏生成 LOD，优先精简不影响轮廓的小倒角、螺钉和内部隐藏细节。
- 用相同相机、灯光和动作片段，对比改动前后的轮廓、开舱连接、解码时间与帧耗时。

验收应同时覆盖视觉质量与设备实测，不预先承诺减面比例。

### 2. 评估共用模型与姿态资产架构

四个 GLB 合计约 7.32 MB，WALK、OPEN 与 PATROL 的网格规模相同。当前按需加载并缓存两个资产，已控制同时保留的资源；下一步可以评估将共用几何、材质和机械层级保存一次，通过姿态或动画切换展示状态。

这需要先核对各文件的节点、几何、骨架 / 运动层级和状态差异。尤其 WHEEL 的节点与网格数量较少，不能只凭命名相似就合并；OPEN 目前是静态导出，也需要重新导出或定义可靠的开舱动作。是否采用共享 GLB、共享层级或独立轻量姿态文件，应由实际收益和维护成本决定。

### 3. 把语义节点变成可探索的结构热点

现有节点命名已经包含光学组件、顶盖、后舱门、双目组件、支撑与轮组，例如 `TKM17_Pod_TopHatch`、`TKM21_FrontOpticSaddle_1` 和 `CTRL_ROOT`。可用这些真实节点挂接少量热点，让“结构档案”与模型联动：

- 点击部件名称，镜头移动到能看清连接关系的观察角度，并适度高亮对应部件。
- OPEN 模式重点解释顶盖支撑、铰链和灯组鞍座；PATROL 模式可定位相关动作时段。
- 让热点随对象运动，支持遮挡判断、键盘访问和移动端展开，避免标签覆盖模型。

先做少量、有明确观察价值的热点。文字必须对应模型实际结构，不补造未经验证的尺寸、性能参数或设定。
